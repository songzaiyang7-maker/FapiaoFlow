# 安全策略

## 报告漏洞

如果你发现安全漏洞，**请不要在公开 issue 里报告**。请通过以下方式私下联系：

- 发邮件到项目维护者的邮箱（请在仓库主页查找）
- 或新建一个**只描述影响、不描述细节**的 issue，等我们联系你

请尽量提供：

- 问题描述和影响范围
- 复现步骤
- 你的联系方式

我们会在收到报告后尽快回复，并在修复后致谢。

## 已知安全考量

### API Key 泄露

本工具通过 `.env` 文件保存 DeepSeek API key。

- `.env` 已加入 `.gitignore`，不会被提交
- 如果你误提交了 API key，**请立即在 DeepSeek 控制台吊销该 key**，然后在 git 历史中清除（`git filter-branch` 或 BFG）
- 即使 key 已从最新提交移除，旧提交历史里仍然有——吊销是唯一可靠的办法

### 发票数据隐私

- 所有发票 PDF 和解析后的文本都保存在本地（`data/sessions.json`），不上传任何外部服务
- 仅当启用 LLM 兜底（hybrid/llm 模式）且配置了 API key 时，发票的**文本片段**（前 1500 字）会发送给 DeepSeek 处理
- 原始 PDF 文件永远不会上传
- `data/` 目录已加入 `.gitignore`，不会被提交

### 建议的防护

如果你要贡献代码，请注意：

- 不要在代码、注释、commit message 里写真实发票信息或 API key
- 测试用例使用合成数据，不要用真实发票
- 如果你录屏/截图做文档，先打码敏感信息（发票号、销售方、金额）
